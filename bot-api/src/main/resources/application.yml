server:
  port: 9000
logging:
  level:
    root: info

spring:
  data:
    mongodb:
      database: bot
      username: root
      password: example
  rsocket:
    server:
      mapping-path: /rsocket
      transport: websocket

  web:
    resources:
      chain:
        cache: false

bot:
  api:
    telegram:
      host: https://api.telegram.org
      timeout: 500
    instagram:
      host: https://graph.facebook.com/v9.0/
      timeout: 500
